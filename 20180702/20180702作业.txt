1、创建一个子线程，子线程申请内存，通过清理函数进行free，子线程停留在read标准输入，主线程cancel子线程，子线程能够通过清理函数free对应的malloc的内存
2、子线程，主线程，同时对一个全局变量加2千万，通过加锁，实现最终效果是4千万,通过gettimeofday统计各加2千万的时间消耗
3、子线程等待在一个条件变量上，主线程发送signal，使条件成立，然后子线程唤醒得到执行，正常结束，然后主线程回收子线程